/** @type {import('next').NextConfig} */
const nextConfig = {
  productionBrowserSourceMaps: true,
  // Preserve any existing Next.js config options
  reactStrictMode: true,
}

module.exports = nextConfig
